version = "1.0"

[metadata]
author_name = "tb.contributor47"
author_email = "tb.contributor47@gmail.com"
difficulty = "medium"
category = "system-administration"
tags = ["permissions", "linux", "ci", "containers", "non-root"]

[verifier]
timeout_sec = 120.0
test_command = "bash -c 'mkdir -p /logs/verifier && chmod 777 /logs/verifier && exec /tests/run_tests.sh'"

[agent]
timeout_sec = 600.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
